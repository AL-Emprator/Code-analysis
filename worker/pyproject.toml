[project]
name = "worker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "AbdulsattarEsmaeil", email = "abdulsattar.esmaeil@stud.hs-bochum.de" }
]
requires-python = ">=3.12"
dependencies = [
    "sqlalchemy>=2.0.52",
]

[project.scripts]
worker = "worker:main"

[build-system]
requires = ["uv_build>=0.12.4,<0.13.0"]
build-backend = "uv_build"
[tool.uv]
package = false